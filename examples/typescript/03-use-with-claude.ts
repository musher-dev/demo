/**
 * Agent SDK demo — coming soon.
 *
 * The Python example (../python/repo_docs_audit.py) demonstrates
 * the full Claude Agent SDK integration including:
 * - Skill discovery from .claude/skills/
 * - Hooks and permission guards
 * - Structured JSON output
 * - Tool-call logging
 *
 * A TypeScript equivalent will be added when the Claude Agent SDK ships
 * a Node.js runtime.
 */

console.log(
	"See examples/python/repo_docs_audit.py for the Agent SDK demo.",
);
